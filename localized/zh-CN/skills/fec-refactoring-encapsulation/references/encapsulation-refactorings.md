# 封装类重构

这些卡片以用户提供的标准重构目录为知识基线，转换为面向前端 Agent 的操作指南。保留标准 ID/中英文名称，框架适配和示例为项目原创内容。

## RF-ENCAPSULATE-RECORD — 封装记录（Encapsulate Record）

### 意图
用明确边界包住裸记录，控制其表示形式和允许的操作。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-DATA-CLASS`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Encapsulate Record”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-ENCAPSULATE-COLLECTION — 封装集合（Encapsulate Collection）

### 意图
避免调用者通过裸集合引用随意修改集合，改由所有者提供受控操作。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- 不要求先存在某一种固定坏味道；以具体结构证据为准。

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Encapsulate Collection”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-REPLACE-PRIMITIVE-WITH-OBJECT — 以对象取代基本类型（Replace Primitive with Object）

### 意图
用领域值抽象承载格式化、校验或不变量，而不是让普通 string/number 在各处重复解释。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-PRIMITIVE-OBSESSION`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Replace Primitive with Object”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-REPLACE-TEMP-WITH-QUERY — 以查询取代临时变量（Replace Temp with Query）

### 意图
把可推导的临时变量替换为查询，减少提炼函数时需要传递的局部状态。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-LONG-FUNCTION`

### 常见组合
- 提炼函数（Extract Function）：仅当下一手法自身前置条件也成立时组合。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Replace Temp with Query”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-EXTRACT-CLASS — 提炼类（Extract Class）

### 意图
把拥有独立数据和行为的职责簇拆到单独所有者中；现代前端不强制使用 class。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 新抽象只会增加跳转、却没有独立数据所有权和变化原因时，不提炼。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-DIVERGENT-CHANGE`
- `SMELL-DATA-CLUMPS`
- `SMELL-PRIMITIVE-OBSESSION`
- `SMELL-TEMPORARY-FIELD`
- `SMELL-LARGE-CLASS`

### 常见组合
- 搬移函数（Move Function）：仅当下一手法自身前置条件也成立时组合。
- 搬移字段（Move Field）：仅当下一手法自身前置条件也成立时组合。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Extract Class”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-INLINE-CLASS — 内联类（Inline Class）

### 意图
当抽象层已失去足够职责时，将其职责合回主要使用者，减少无价值间接层。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-SHOTGUN-SURGERY`
- `SMELL-LAZY-ELEMENT`
- `SMELL-SPECULATIVE-GENERALITY`

### 常见组合
- `RF-EXTRACT-CLASS`：仅当下一手法自身前置条件也成立时组合。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Inline Class”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-HIDE-DELEGATE — 隐藏委托关系（Hide Delegate）

### 意图
在稳定边界上提供所需操作，避免客户端依赖内部对象导航路径。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-MESSAGE-CHAINS`
- `SMELL-INSIDER-TRADING`

### 常见组合
- `RF-REMOVE-MIDDLE-MAN`：仅当下一手法自身前置条件也成立时组合。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Hide Delegate”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-REMOVE-MIDDLE-MAN — 移除中间人（Remove Middle Man）

### 意图
当中间层主要只是转发时，让调用者在可控范围内直接访问真正的协作者。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-MIDDLE-MAN`

### 常见组合
- `RF-HIDE-DELEGATE`：仅当下一手法自身前置条件也成立时组合。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Remove Middle Man”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-SUBSTITUTE-ALGORITHM — 替换算法（Substitute Algorithm）

### 意图
在锁定输入输出、顺序、异常和性能敏感边界后，用更清晰合适的算法替换旧实现。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- 不要求先存在某一种固定坏味道；以具体结构证据为准。

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 检查对象身份、属性语义、迭代顺序、可变性和序列化。

### TypeScript 注意事项
- 按真实不变量使用 `readonly`、窄接口或 value type；DTO 转换边界必须显式。

### React 注意事项
- 保持 props/state 身份、memoization、context 和受控/非受控契约。

### Vue 注意事项
- 保持响应式代理身份、computed 追踪、store action、props/emits 和序列化边界。

### Frontend-Craft 前端适配
- 封装可以落为 module API、service、hook/composable、store slice、domain object 或 value abstraction。

### 原创前端变换示例
- 把“Substitute Algorithm”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。
