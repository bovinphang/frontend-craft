# 搬移特性类重构

这些卡片以用户提供的标准重构目录为知识基线，转换为面向前端 Agent 的操作指南。保留标准 ID/中英文名称，框架适配和示例为项目原创内容。

## RF-MOVE-FUNCTION — 搬移函数（Move Function）

### 意图
把函数搬到拥有其主要数据或变化责任的模块。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-DIVERGENT-CHANGE`
- `SMELL-SHOTGUN-SURGERY`
- `SMELL-FEATURE-ENVY`
- `SMELL-TEMPORARY-FIELD`
- `SMELL-MESSAGE-CHAINS`
- `SMELL-INSIDER-TRADING`
- `SMELL-ALTERNATIVE-CLASSES-DIFFERENT-INTERFACES`
- `SMELL-DATA-CLASS`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Move Function”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-MOVE-FIELD — 搬移字段（Move Field）

### 意图
把数据搬到真正拥有该概念、并与其共同变化的对象或模块。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-SHOTGUN-SURGERY`
- `SMELL-INSIDER-TRADING`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Move Field”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-MOVE-STATEMENTS-INTO-FUNCTION — 搬移语句到函数（Move Statements into Function）

### 意图
把所有调用者都必须伴随执行的公共语句移入被调用函数，使公共操作完整。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- 不要求先存在某一种固定坏味道；以具体结构证据为准。

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Move Statements into Function”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-MOVE-STATEMENTS-TO-CALLERS — 搬移语句到调用者（Move Statements to Callers）

### 意图
把只对部分调用者成立或因上下文而变化的语句移回调用端。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- 不要求先存在某一种固定坏味道；以具体结构证据为准。

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Move Statements to Callers”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-REPLACE-INLINE-CODE-WITH-FUNCTION-CALL — 以函数调用取代内联代码（Replace Inline Code with Function Call）

### 意图
用已经存在且语义等价的函数调用替代重复的内联逻辑。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- 不要求先存在某一种固定坏味道；以具体结构证据为准。

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Replace Inline Code with Function Call”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-SLIDE-STATEMENTS — 移动语句（Slide Statements）

### 意图
在不跨越可观察依赖的前提下移动语句，让相关代码相邻，为后续提炼或简化创造条件。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-DUPLICATED-CODE`
- `SMELL-MUTABLE-DATA`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Slide Statements”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-SPLIT-LOOP — 拆分循环（Split Loop）

### 意图
把同时承担多个独立任务的循环拆成多个聚焦循环。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-LONG-FUNCTION`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Split Loop”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-REPLACE-LOOP-WITH-PIPELINE — 以管道取代循环（Replace Loop with Pipeline）

### 意图
当循环本质是集合过滤、映射或聚合时，用意图更清晰的管道表达。

### 典型信号
- 当前结构出现与本手法目标一致的重复、耦合、命名、职责、数据所有权或控制流问题，并有具体代码证据。

### 不适用条件
- 当前结构已经清晰，或该变换会制造更强耦合、隐藏依赖、破坏公共契约且没有兼容方案时，不使用。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-LOOPS`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 把“Replace Loop with Pipeline”应用到一个局部组件/Hook/Composable/Service 范围内，先记录行为契约，只迁移一个职责片段，并立即验证输出、事件与状态变化保持不变。

## RF-REMOVE-DEAD-CODE — 移除死代码（Remove Dead Code）

### 意图
移除已经证明不存在运行时或受支持工具链用途的代码；实际发现和删除由现有 cleanup lane 执行。

### 典型信号
- 代码疑似无引用，但必须进一步检查 dynamic import、route、Storybook、i18n key、Tailwind 动态 class、runtime template 和外部消费者。

### 不适用条件
- 没有完成动态引用与外部消费者证明时，不执行删除。

### 前置条件
- 已明确当前可观察行为、受影响调用者和公共契约。
- 目标位于声明的差异预算内，并且这个单一主要变换完成后能够立即验证。

### 操作步骤
1. 确认当前行为、调用者和边界，并锁定本步骤只处理这一项主要结构意图。
2. 创建最小的新结构/位置/接口，让旧行为可以逐步迁移而不是一次性大改。
3. 一次迁移一个调用点或职责片段，并保持兼容边界。
4. 运行窄范围验证；若变红，先回滚本步骤，再缩小变换。

### 行为保持检查点
- 对比输入、输出、异常、副作用、执行顺序和公共契约。
- 前端还要核对 UI 状态、事件、焦点、网络请求、生命周期/状态迁移，以及相关对象身份语义。

### 相关坏味道
- `SMELL-SPECULATIVE-GENERALITY`

### 常见组合
- 不建立固定流水线；下一步必须重新依据证据选择。

### JavaScript 注意事项
- 模块初始化顺序、循环依赖、闭包和动态 import 会让“搬文件”变成行为变化。

### TypeScript 注意事项
- 保持导出类型、路径别名、包边界以及 type-only/runtime import 区别。

### React 注意事项
- 检查 Hook 规则、Server/Client 边界、lazy/Suspense、路由和事件顺序。

### Vue 注意事项
- 检查 composable 生命周期、Pinia 所有权、async setup、route guard 和 auto-import。

### Frontend-Craft 前端适配
- 行为应跟随数据所有权和变化责任；死代码实际删除必须经过专门 cleanup lane 的动态引用证明。

### 原创前端变换示例
- 某组件看似未使用时，先检查 lazy import、路由、Storybook、i18n、动态样式和 runtime template；实际删除交给 `fec-refactor-clean`。
