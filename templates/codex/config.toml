# Frontend Craft - Codex project configuration
# Copy to .codex/config.toml and adjust according to project needs

# MCP design tool integration (uncomment and configure environment variables as needed)
# Required settings: FIGMA_API_KEY, SKETCH_API_KEY, MG_MCP_TOKEN, MODAO_TOKEN

# [mcp_servers.figma]
# command = "npx"
# args = ["-y", "figma-developer-mcp", "--stdio"]
# env = { FIGMA_API_KEY = "${FIGMA_API_KEY}" }

# [mcp_servers.figma-desktop]
# command = "npx"
# args = ["-y", "@anthropic/figma-mcp"]
# env = { FIGMA_API_KEY = "${FIGMA_API_KEY}" }

# [mcp_servers.sketch]
# command = "npx"
# args = ["-y", "sketch-mcp-server", "--stdio"]
# env = { SKETCH_API_KEY = "${SKETCH_API_KEY}" }

# [mcp_servers.mastergo]
# command = "npx"
# args = ["-y", "@mastergo/magic-mcp", "--token=${MG_MCP_TOKEN}", "--url=https://mastergo.com"]

# [mcp_servers.pixso]
# url = "http://localhost:3667/mcp"

# [mcp_servers.modao]
# command = "npx"
# args = ["-y", "@modao-mcp/modao-proto-mcp"]
# env = { MODAO_TOKEN = "${MODAO_TOKEN}" }
